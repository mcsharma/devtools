{
  "name": "SearchWebApp",
    "version": "0.0.1",
    "author": "Mahesh Sharma",
    "description": "A web app for searching code in a repository",
    "dependencies": {
    },
    "devDependencies": {
      "browserify": "latest",
      "watchify": "latest"
    },
    "scripts": {
      "buildjs": "browserify -t [ babelify --presets [ react ] ] main.js -o bundle.js -d -v",
      "watchjs": "watchify -t [ babelify --presets [ react ] ] main.js -o bundle.js -d -v"
    }
}
